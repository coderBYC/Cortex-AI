# Phase 1 — Local-First Memory Engine
# Requires Python 3.11+

numpy>=1.26.0
jellyfish>=1.0.0
sqlite-vec>=0.1.6

# Real local embeddings (ONNX / MiniLM-class — replaces hashing-trick vectors)
fastembed>=0.4.0

# Local LLM JSON extraction with GBNF grammars
llama-cpp-python>=0.2.90

# LoCoMo evaluation scoring
nltk>=3.8
regex>=2023.0.0

# Optional: OpenAI answer head for A/B vs local llama (--openai-model)
openai>=1.40.0
